apiVersion: v1
kind: Service
metadata:
  name: my-service
  labels:
    app: my-app
spec:
  selector:
    app: my-app
  type: NodePort
  ports:
  - nodePort : 30100
    protocol: TCP
    port: 80 
    targetPort: 443

